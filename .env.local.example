# Vercel configuration (required)
VERCEL_ENV=development

# Google Cloud Storage configuration (required)
GCS_BUCKET_NAME=your-gcs-bucket-name

# Google Cloud Project ID (optional, will use default if not specified)
GCP_PROJECT_ID=your-gcp-project-id

# Google Cloud Service Account credentials (required for production)
# JSON format service account key, for local development you can use ADC
GCP_SERVICE_ACCOUNT={"type":"service_account",...}

# Firebase configuration (required)
FIREBASE_DATABASE_URL=https://db-default-rtdb.firebaseio.com
FIREBASE_PROJECT_ID=db